import { ApplyBorderDirective } from './apply-border.directive';

describe('ApplyBorderDirective', () => {
  it('should create an instance', () => {
    const directive = new ApplyBorderDirective();
    expect(directive).toBeTruthy();
  });
});
